# Public Supabase project settings (safe to expose to the browser where required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only configuration. Never expose or commit a real value.
SUPABASE_SERVICE_ROLE_KEY=

# Public site settings
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Future external adapters (leave blank until sandbox credentials are available)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_WEBHOOK_ID=
BILLING_PROVIDER=
