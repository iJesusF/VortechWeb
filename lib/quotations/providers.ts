export type CheckoutInput = { quoteId: string; amountCents: number; currency: "MXN"; idempotencyKey: string };
export interface PaymentProvider { createCheckoutSession(input: CheckoutInput): Promise<{ externalReference: string; checkoutUrl: string }>; getCheckoutUrl(reference: string): Promise<string | null>; getPaymentStatus(reference: string): Promise<"pending" | "completed" | "failed" | "expired">; verifyWebhook(payload: string, signature: string | null): Promise<boolean>; cancelSession(reference: string): Promise<void>; }
export interface BillingProvider { validateCustomerTaxData(input: { rfc?: string; businessName?: string; fiscalZipCode?: string }): Promise<{ valid: boolean; errors: string[] }>; createInvoice(): Promise<never>; getInvoiceStatus(): Promise<"unconfigured">; downloadPdf(): Promise<never>; downloadXml(): Promise<never>; cancelInvoice(): Promise<never>; }
export class MockPaymentProvider implements PaymentProvider { async createCheckoutSession(input: CheckoutInput) { return { externalReference: `mock_${input.idempotencyKey}`, checkoutUrl: `/admin/cotizaciones/${input.quoteId}?payment=mock` }; } async getCheckoutUrl() { return null; } async getPaymentStatus() { return "pending" as const; } async verifyWebhook() { return false; } async cancelSession() {} }
export class MockBillingProvider implements BillingProvider { async validateCustomerTaxData(input: { rfc?: string; businessName?: string; fiscalZipCode?: string }) { const errors = [input.rfc, input.businessName, input.fiscalZipCode].every(Boolean) ? [] : ["Faltan datos fiscales."]; return { valid: errors.length === 0, errors }; } async createInvoice(): Promise<never> { throw new Error("Integración no configurada."); } async getInvoiceStatus() { return "unconfigured" as const; } async downloadPdf(): Promise<never> { throw new Error("Integración no configurada."); } async downloadXml(): Promise<never> { throw new Error("Integración no configurada."); } async cancelInvoice(): Promise<never> { throw new Error("Integración no configurada."); } }
