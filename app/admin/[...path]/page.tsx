import Link from "next/link";
export default async function AdminSection({ params }: { params: Promise<{ path: string[] }> }) { const { path } = await params; return <main className="container-shell py-20"><Link href="/admin" className="text-sm font-semibold text-cyanx">← Administración</Link><span className="section-eyebrow mt-8">Configuración requerida</span><h1 className="section-title">{path.join(" / ")}</h1><p className="section-copy">La interfaz y el modelo de datos están preparados. Para operar esta sección se requiere aplicar la migración, configurar Supabase y registrar un usuario administrativo. No se muestran ni modifican datos hasta contar con esa autorización.</p></main>; }
